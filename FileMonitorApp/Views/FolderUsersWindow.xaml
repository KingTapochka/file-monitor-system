<Window x:Class="FileMonitorApp.Views.FolderUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Monitor - Проверка папки" 
        Height="550" 
        Width="850"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEE"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
        </Style>
        
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
        </Style>
        
        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="TitleText"
                       Text="📁 Проверка папки" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       Foreground="#333"/>
        </StackPanel>
        
        <!-- Путь к папке -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#DDD" 
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,10">
            <TextBlock x:Name="FolderPathText"
                       Text="Путь к папке..."
                       FontSize="13"
                       Foreground="#666"
                       TextTrimming="CharacterEllipsis"
                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </Border>

        <!-- Статус -->
        <Border x:Name="StatusBorder" 
                Grid.Row="2" 
                Background="#E8F5E9" 
                BorderBrush="#4CAF50" 
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,15"
                Visibility="Collapsed">
            <TextBlock x:Name="StatusText"
                       Text="Статус..."
                       FontSize="13"
                       Foreground="#2E7D32"/>
        </Border>

        <!-- Контент (таблица / сообщения) -->
        <Grid Grid.Row="3">
            <!-- Индикатор загрузки -->
            <StackPanel x:Name="LoadingPanel" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center">
                <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock x:Name="LoadingText"
                           Text="Сканирование папки..." 
                           FontSize="14" 
                           Foreground="#666"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
            
            <!-- Таблица занятых файлов -->
            <DataGrid x:Name="FilesDataGrid"
                      Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource HeaderStyle}"
                      CellStyle="{StaticResource CellStyle}"
                      Visibility="Collapsed">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Файл" Binding="{Binding FileName}" Width="*" MinWidth="200"/>
                    <DataGridTextColumn Header="Пользователь" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="Компьютер" Binding="{Binding ClientName}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <!-- Сообщение: папка свободна -->
            <StackPanel x:Name="NoUsersPanel"
                        Visibility="Collapsed"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center">
                <TextBlock Text="✅" FontSize="64" HorizontalAlignment="Center"/>
                <TextBlock Text="Все файлы в папке свободны" 
                           FontSize="16" 
                           Foreground="#4CAF50"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
                <TextBlock Text="Ни один файл не используется другими пользователями" 
                           FontSize="13" 
                           Foreground="#666"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Сообщение об ошибке -->
            <StackPanel x:Name="ErrorPanel"
                        Visibility="Collapsed"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center">
                <TextBlock Text="❌" FontSize="64" HorizontalAlignment="Center"/>
                <TextBlock Text="Ошибка при сканировании" 
                           FontSize="16" 
                           Foreground="#F44336"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
                <TextBlock x:Name="ErrorMessage"
                           Text="Не удалось подключиться к серверу" 
                           FontSize="13" 
                           Foreground="#666"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <CheckBox x:Name="IncludeSubfoldersCheckBox"
                      Content="Включая подпапки"
                      VerticalAlignment="Center"
                      Margin="0,0,20,0"
                      IsChecked="True"/>
            <Button x:Name="RefreshButton"
                    Content="🔄 Обновить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="120"
                    Click="RefreshButton_Click"/>
            <Button Content="Закрыть" 
                    Width="100" 
                    Margin="10,0,0,0"
                    Padding="15,8"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
