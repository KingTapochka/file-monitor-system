<Window x:Class="FileMonitorApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Monitor - Настройки" 
        Height="280" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⚙️ Настройки" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       Foreground="#333"/>
            <TextBlock Text="Конфигурация подключения к серверу" 
                       FontSize="12" 
                       Foreground="#666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Настройки -->
        <StackPanel Grid.Row="1">
            <!-- Адрес сервера -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Адрес сервера мониторинга:" 
                           FontSize="13" 
                           Foreground="#666"
                           Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="ServerAddressTextBox" 
                             Grid.Column="0"
                             Padding="10,8"
                             FontSize="13"
                             BorderBrush="#CCC"
                             BorderThickness="1"/>
                    <Button Grid.Column="1" 
                            Content="🔗 Тест" 
                            Padding="10,8"
                            Margin="8,0,0,0"
                            Click="TestConnectionButton_Click"/>
                </Grid>
                <TextBlock Text="Пример: http://server-name:5000" 
                           FontSize="11" 
                           Foreground="#999"
                           Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Статус подключения -->
            <Border Background="White" 
                    BorderBrush="#DDD" 
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="ConnectionStatusIcon"
                               Text="⚪" 
                               FontSize="16"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="ConnectionStatusText"
                               Text="Статус подключения неизвестен" 
                               FontSize="13"
                               Foreground="#666"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="💾 Сохранить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="120"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена" 
                    Width="100" 
                    Margin="10,0,0,0"
                    Padding="15,8"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
