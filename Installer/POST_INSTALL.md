# ⚠️ ВАЖНО: ИНСТРУКЦИЯ ПОСЛЕ УСТАНОВКИ MSI

## Проблема
После установки MSI клиента **НЕ ПОЯВЛЯЕТСЯ** пункт в контекстном меню Explorer.

## Причина
MSI установщик **НЕ регистрирует** Shell Extension автоматически, так как:
1. `regasm.exe` НЕ работает правильно для SharpShell COM объектов
2. Автоматический перезапуск Explorer ломает панель задач

## ✅ Решение

### После установки MSI ОБЯЗАТЕЛЬНО выполните:

1. **Откройте PowerShell ОТ ИМЕНИ АДМИНИСТРАТОРА**

2. **Перейдите в папку проекта:**
```powershell
cd "C:\monitoring_polzovateley"
```

3. **Запустите скрипт регистрации:**
```powershell
.\Scripts\Register-ShellExtension.ps1
```

4. **Explorer перезапустится автоматически** (все окна закроются и откроются)

5. **Проверьте работу:**
   - Откройте любую папку
   - ПКМ на файле
   - Должен появиться пункт "Кто использует файл?"

---

## 🔧 Если панель задач пропала

Это происходит если MSI пытался перезапустить Explorer. **Решение:**

```powershell
# Запустите Explorer вручную
Start-Process explorer.exe

# Или перезагрузите компьютер
Restart-Computer
```

---

## 📝 Правильная последовательность установки

### Вариант А: Полная установка (сервер + клиент)

```powershell
# 1. Установка MSI
msiexec /i "C:\monitoring_polzovateley\Installer\FileMonitorSetup.msi"

# 2. Регистрация Shell Extension (от администратора)
cd "C:\monitoring_polzovateley"
.\Scripts\Register-ShellExtension.ps1

# 3. Проверка системы
.\Scripts\Test-System.ps1
```

### Вариант Б: Только сервер (без клиента)

```powershell
# Установка только сервера
msiexec /i "C:\monitoring_polzovateley\Installer\FileMonitorSetup.msi" ADDLOCAL=ServerFeature /qb

# Проверка
.\Scripts\Test-System.ps1
```

### Вариант В: Только клиент (на рабочих станциях)

```powershell
# 1. Установка MSI
msiexec /i "C:\monitoring_polzovateley\Installer\FileMonitorSetup.msi" ADDLOCAL=ClientFeature /qb

# 2. ОБЯЗАТЕЛЬНО: Регистрация Shell Extension
cd "C:\monitoring_polzovateley"
.\Scripts\Register-ShellExtension.ps1

# 3. Настройка адреса сервера
# Отредактируйте: C:\Program Files\File Monitor System\Client\FileMonitorClient.dll.config
# Укажите IP вашего сервера вместо localhost
```

---

## 🚀 Быстрая установка (одна команда)

```powershell
# Установка + регистрация + проверка
msiexec /i "C:\monitoring_polzovateley\Installer\FileMonitorSetup.msi" /qb; Start-Sleep 5; cd "C:\monitoring_polzovateley"; .\Scripts\Register-ShellExtension.ps1; .\Scripts\Test-System.ps1
```

---

## 📋 Сборка нового MSI

После любых изменений пересоберите установщик:

```powershell
.\Scripts\Build-Installer.ps1
```

Новый MSI будет в: `Installer\FileMonitorSetup.msi`

---

## 🆘 Если ничего не помогает

1. **Удалите MSI:**
```powershell
msiexec /x "C:\monitoring_polzovateley\Installer\FileMonitorSetup.msi" /qn
```

2. **Отмените регистрацию Shell Extension:**
```powershell
.\Scripts\Unregister-ShellExtension.ps1
```

3. **Перезагрузите компьютер**

4. **Установите заново** (см. Вариант А выше)

---

## ℹ️ Дополнительная информация

- **START_INSTALL.md** - Полная инструкция по установке
- **TROUBLESHOOTING.md** - Решение проблем
- **Scripts/Test-System.ps1** - Проверка всех компонентов
- **Scripts/Register-ShellExtension.ps1** - Регистрация клиента
- **Scripts/Unregister-ShellExtension.ps1** - Удаление регистрации

---

## ✅ Проверка что всё работает

```powershell
# 1. Проверка установки
Get-Service FileMonitorService
Get-ChildItem "C:\Program Files\File Monitor System"

# 2. Проверка API
Invoke-RestMethod http://localhost:5000/api/files/health

# 3. Проверка Shell Extension
# Откройте Explorer, ПКМ на файле → должен быть пункт "Кто использует файл?"

# 4. Полная проверка
.\Scripts\Test-System.ps1
```
